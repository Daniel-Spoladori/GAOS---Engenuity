-- cd isnt used anymore since it is not very necessary, well thats is it it

-- increment that count of time trying fix it by 1 at every hour
-- hours trying make this work [10] (study included)

print("Change Dir is decrapted!")


-- for i, v in pairs(component.methods(address)) do
--     -- print(i)
-- end

-- function TermCannonical(directory, paramaters)
--     directory = tostring(directory)
--     print("diretorio = "..directory)
--     local segments = TermSegment(directory)
--     local runned = false

--     for index, value in pairs(segments) do

--         if value == "." then
--             table.remove(segments,index) -- "kills" the .
--         end


--         if value == ".." then
--             table.remove(segments,index) -- "kills" the ..
--             table.remove(segments,index-1) -- "kills" the previus path

--         end

--     end
--     return segments
-- end
-- -- print("Write the directory bellow:")
-- print("Currently at: "..terminal.path.path)

-- for  i,v in pairs(TermCannonical("bin/xuxu/../documents")) do
--     print(v)
-- end


-- -- local directory = terminalInput(true)

-- -- print("You moved to: "..terminal.path.path)